/*using UnityEngine;

public class LootChance : MonoBehaviour{

    bool dead = false;
    bool alive = true;
    [SerializeField] Transform Zombie;

    void Start()
    {
        dead = false;
        alive = true;
    }

    void Update()
    {
        if (dead == true)
        {
            Zombie = new Vector3(gameObject.transform., gameObject.transform, -.5f);
           gameObject(Destroy);

        }
    }
}
*/